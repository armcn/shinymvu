% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{list_update_in}
\alias{list_update_in}
\title{Update a Value at a Nested Path with a Function}
\usage{
list_update_in(.l, .path, .fn)
}
\arguments{
\item{.l}{A list.}

\item{.path}{A character vector of keys forming the path.}

\item{.fn}{A function taking the current value and returning the new
value.}
}
\value{
A new list with the value at the path transformed by \code{.fn}.
}
\description{
Reads the current value at \code{.path}, applies \code{.fn} to it, and writes
the result back. If the path does not exist, \code{.fn} receives \code{NULL}.
}
\examples{
model <- list(counters = list(a = 1, b = 5))
list_update_in(model, c("counters", "a"), function(x) x + 1)
list_update_in(model, c("counters", "c"), function(x) if (is.null(x)) 0 else x + 1)

}
