% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/devtools.R
\name{mvu_replay}
\alias{mvu_replay}
\title{Replay a Transition Log Through an Update Function}
\usage{
mvu_replay(init, update, log)
}
\arguments{
\item{init}{A function returning the initial model, or a model list.}

\item{update}{The update function.}

\item{log}{An \code{mvu_log} object, or a plain list of transitions.}
}
\value{
A list of models of length \code{n + 1} where \code{n} is the number
of transitions. Element 1 is the initial model, element \code{n + 1} is
the final model.
}
\description{
Re-runs every message from \code{log} through \code{update}, starting from
\code{init}. Returns a list of models — one per step plus the initial
state — so you can inspect the model at any point in the history.
}
\details{
Effects are collected but not executed; inspect them on the returned
\code{mvu_result} objects if needed.
}
\examples{
\dontrun{
models <- mvu_replay(my_init, my_update, saved_log)
models[[1]] # initial state
models[[length(models)]] # final state
}

}
