% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{effect_cmd}
\alias{effect_cmd}
\title{Create an Async Command Effect}
\usage{
effect_cmd(fn, msg, error_msg = NULL)
}
\arguments{
\item{fn}{A function with no arguments that performs the work. Runs
in a background R process.}

\item{msg}{The message type (string) to dispatch when \code{fn} completes
successfully. The return value of \code{fn} becomes the message \code{value}.}

\item{error_msg}{Optional message type to dispatch on failure. The
error message string becomes the \code{value}. If \code{NULL} (default),
errors are silently ignored.}
}
\value{
An \code{mvu_effect} object.
}
\description{
Describes an asynchronous operation that runs in a background process
and dispatches a message with the result when complete. This is the
Elm-style \code{Cmd} pattern: the app stays interactive while the work
runs, and the result feeds back through \code{update} as a new message.
}
\details{
Requires the \pkg{future} package to be installed and a
\code{\link[future:plan]{future::plan()}} to be set (e.g. \code{future::plan(future::multisession)}).
}
\examples{
effect_cmd(
  fn = function() head(mtcars, 5),
  msg = "data_loaded",
  error_msg = "load_failed"
)

}
