% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{mvu_result}
\alias{mvu_result}
\title{Return Model and Effects from Update}
\usage{
mvu_result(model, ...)
}
\arguments{
\item{model}{The new model (a list).}

\item{...}{Zero or more effect objects created by \code{\link[=effect_notify]{effect_notify()}},
\code{\link[=effect_send]{effect_send()}}, \code{\link[=effect_custom]{effect_custom()}}, or grouped with \code{\link[=effects]{effects()}}.}
}
\value{
An \code{mvu_result} S3 object with \verb{$model} and \verb{$effects} fields.
}
\description{
Wraps a new model together with side effects that the runtime should
execute after the state transition. This is the Elm-style "command"
pattern: \code{update} stays pure by \emph{describing} effects as data rather
than performing them.
}
\details{
When \code{update} returns a plain list, the runtime treats it as a model
with no effects (backward compatible). When it returns an \code{mvu_result},
the runtime updates the model and then runs the effects.
}
\examples{
# Model-only (equivalent to returning the model directly)
mvu_result(list(count = 1))

# Model with a notification
mvu_result(
  list(count = 1),
  effect_notify("Saved!", type = "message")
)

# Model with multiple effects
mvu_result(
  list(count = 0),
  effect_notify("Reset complete"),
  effect_send("analytics", list(event = "reset"))
)

}
