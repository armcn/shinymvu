% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view.R
\name{mvu_tag}
\alias{mvu_tag}
\title{Build an HTML Element with MVU Bindings}
\usage{
mvu_tag(
  .tag,
  ...,
  text = NULL,
  show = NULL,
  on_click = NULL,
  on_input = NULL,
  on_change = NULL,
  value = NULL,
  checked = NULL,
  debounce = NULL,
  .bindings = list()
)
}
\arguments{
\item{.tag}{Tag name as a string (e.g. \code{"div"}, \code{"button"}, \code{"input"})
or an htmltools tag function (e.g. \code{tags$button}).}

\item{...}{Children and standard HTML attributes.}

\item{text}{Bind the element's text content to a model expression.}

\item{show}{Conditionally show/hide the element based on a model
expression.}

\item{on_click}{Dispatch this \code{\link[=msg]{msg()}} when the element is clicked.}

\item{on_input}{Dispatch this \code{\link[=msg]{msg()}} when the input value changes.
If the message has no explicit payload, the input value is sent
automatically.}

\item{on_change}{Dispatch this \code{\link[=msg]{msg()}} on change events (checkboxes,
selects).}

\item{value}{Bind the input's value to a model expression.}

\item{checked}{Bind a checkbox's checked state to a model expression.}

\item{debounce}{Debounce time in milliseconds for \code{on_input}. Useful
for text fields to avoid sending a message on every keystroke.}

\item{.bindings}{Named list of additional dynamic attribute bindings.
Names are attribute names; values are model expressions.}
}
\value{
An htmltools tag.
}
\description{
Creates an htmltools tag with semantic MVU bindings. This is the
primary building block for view functions. Standard htmltools
attributes and children are passed through; MVU-specific named
parameters are translated to runtime bindings internally.
}
\examples{
m <- model_ref()
mvu_tag("span", text = m$count)
mvu_tag("button", "Click me", on_click = msg("clicked"))
mvu_tag("input", type = "text", value = m$name,
  on_input = msg("set_name"), debounce = 300)

}
