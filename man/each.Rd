% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view.R
\name{each}
\alias{each}
\title{Render a List of Items}
\usage{
each(items, fn, key = NULL, as = "item")
}
\arguments{
\item{items}{A model reference pointing to the list (e.g. \code{m$todos}).}

\item{fn}{A function that takes an item proxy and returns an htmltools
tag.}

\item{key}{Optional function that takes the item proxy and returns a
reference to use as the unique key for efficient DOM updates.}

\item{as}{Variable name for the iteration item. Defaults to \code{"item"}.
Change this when nesting \code{each()} calls.}
}
\value{
An htmltools \verb{<template>} tag with list rendering.
}
\description{
Iterates over a list in the model and renders each item using a
view function. The view function receives a model reference proxy
for the current item.
}
\examples{
m <- model_ref()
each(m$items, function(item) {
  mvu_tag("li", text = item$name)
}, key = function(item) item$id)

}
