% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view.R
\name{when}
\alias{when}
\title{Conditional Rendering}
\usage{
when(cond, ...)
}
\arguments{
\item{cond}{A model reference or \code{\link[=raw_js]{raw_js()}} expression that evaluates
to a boolean.}

\item{...}{Child elements to render conditionally.}
}
\value{
An htmltools \verb{<template>} tag.
}
\description{
Renders its children only when \code{cond} is truthy. The element is fully
removed from the DOM when the condition is false (unlike \code{show} in
\code{\link[=mvu_tag]{mvu_tag()}}, which uses CSS display).
}
\examples{
m <- model_ref()
when(m$loading, shiny::div("Loading..."))
when(raw_js("model.items.length === 0"), shiny::tags$p("No items"))

}
