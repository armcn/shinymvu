% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enum.R
\name{match_enum}
\alias{match_enum}
\title{Exhaustive Pattern Matching on Enum Values}
\usage{
match_enum(enum, ..., .default)
}
\arguments{
\item{enum}{A \code{mvu_enum} object created by a factory from \code{\link[=mvu_enum]{mvu_enum()}}.}

\item{...}{Formulas of the form \code{"value" ~ expression} or
\code{c("a", "b") ~ expression}. The left-hand side specifies which enum
values to match; the right-hand side is the expression to evaluate.}

\item{.default}{An optional default expression evaluated when no case
matches. When omitted, all enum levels must have a corresponding case.}
}
\value{
The result of evaluating the matched expression.
}
\description{
Matches a \code{mvu_enum} value against a set of cases defined using formula
syntax. Without \code{.default}, all enum levels must be handled, ensuring
exhaustive matching at runtime.
}
\examples{
check_msg <- mvu_enum(c("increment", "decrement", "reset"))
model <- list(count = 5)

msg <- check_msg("increment")
match_enum(msg,
  "increment" ~ list_set(model, count = model$count + 1),
  "decrement" ~ list_set(model, count = model$count - 1),
  "reset"     ~ list_set(model, count = 0)
)

msg <- check_msg("reset")
match_enum(msg,
  "reset" ~ list_set(model, count = 0),
  .default = model
)

}
